# Project 1: Threads

## Preliminaries

>Fill in your name and email address.

Junshi Liu (刘君实) <shshshsh@stu.pku.edu.cn>
>If you have any preliminary comments on your submission, notes for the
>TAs, please give them here.

没啥好说的，其实有那么一点点抓瞎。

>Please cite any offline or online sources you consulted while
>preparing your submission, other than the Pintos documentation, course
>text, lecture notes, and course staff.

大模型很厉害！！！

## Alarm Clock

#### DATA STRUCTURES

>A1: Copy here the declaration of each new or changed struct or struct member, global or static variable, typedef, or enumeration.  Identify the purpose of each in 25 words or less.

```c
// thread.h
struct thread
  {
    /* Owned by thread.c. */
    tid_t tid;                          /**< Thread identifier. */
    enum thread_status status;          /**< Thread state. */
    char name[16];                      /**< Name (for debugging purposes). */
    uint8_t *stack;                     /**< Saved stack pointer. */
    int priority;                       /**< Priority. */
    struct list_elem allelem;           /**< List element for all threads list. */
    int64_t wakeup_tick;                /**< Tick to wake up on. */

    /* Shared between thread.c and synch.c. */
    struct list_elem elem;              /**< List element. */

    /* Used by timer.c, sleeping_list. */
    struct list_elem sleep_elem;        /**< List element for sleeping list. */

#ifdef USERPROG
    /* Owned by userprog/process.c. */
    uint32_t *pagedir;                  /**< Page directory. */
#endif

    /* Owned by thread.c. */
    unsigned magic;                     /**< Detects stack overflow. */
  };
```

```c
// timer.c
/** Number of timer ticks since OS booted. */
extern int64_t ticks;

/** List of threads that are sleeping. */
static struct list sleeping_list;
```

`wakeup_tick` 用来记录线程应该何时被唤醒，数值较小的优先调度（22 字）。  
`sleep_elem` 给 `sleeping_list` 留下元素空间。   
`sleeping_list` 用来存储休眠线程并按照唤醒时间从小到大排序。   
`ticks` 参见注释。

#### ALGORITHMS

>A2: Briefly describe what happens in a call to timer_sleep(),
>including the effects of the timer interrupt handler.

首先禁用中断，防止休眠线程插入 `sleeping_list` 时发生中断。然后计算出唤醒时间插入到有序等待列表中，并将该线程设为阻塞，然后启用中断。

>A3: What steps are taken to minimize the amount of time spent in
>the timer interrupt handler?

维护有序列表，按唤醒时间从小到大排序。这样可以避免遍历整个线程列表，只需要查看列表头就可以确定这个线程需不需要唤醒。

#### SYNCHRONIZATION

>A4: How are race conditions avoided when multiple threads call
>timer_sleep() simultaneously?

出现竞争会影响全局数据结构 `sleeping_list`。由于我们在修改列表之前禁用了中断，而 `sleeping_list` 的修改发生在中断禁用的临界区，故不会发生竞争。

>A5: How are race conditions avoided when a timer interrupt occurs
>during a call to timer_sleep()?

和上面差不多。我们把时钟中断也给禁用了，所以要等 `timer_sleep()` 执行完之后才会进行中断处理。

#### RATIONALE

>A6: Why did you choose this design?  In what ways is it superior to
>another design you considered?

当前这个做法比较直观，速度快，无错误。非要找个东西比较的话，这里不能用锁，因为时钟中断不能等待锁。

## Priority Scheduling

#### DATA STRUCTURES

>B1: Copy here the declaration of each new or changed struct or struct member, global or static variable, typedef, or enumeration.  Identify the purpose of each in 25 words or less.

```c
// thread.h
#define max(a, b) ((a) > (b) ? (a) : (b))
struct thread
  {
    ...
    int base_priority;                  /**< Base priority before donations. */
    int donated_priority;               /**< Priority donated by other threads. */
    struct lock *waiting_lock;          /**< Lock the thread is waiting on, if any. */
    struct list locks;                  /**< List of locks held by the thread. */
    ...
  }
```

`base_priority` 是进程的原始优先级。   
`donated_priority` 进程捐赠优先级，记录其他线程捐献给该线程的最高优先级（25 字）。   
`waiting_lock` 线程等待的锁。   
`locks` 持有的锁的列表。

宏命令的含义比较明显。

```c
// synch.h
#define MAX_DONATION_DEPTH 8
struct lock 
  {
    ...
    int max_priority;           /**< Maximum priority of threads waiting for this lock. */
    struct list_elem elem;      /**< List element for priority donation. */
  };
```

`max_priority` 记录等待该锁的最大优先级。   
`elem` 是用来快速遍历线程持有的锁的列表的。   
`MAX_DONATION_DEPTH` 如其名。

>B2: Explain the data structure used to track priority donation.
>Use ASCII art to diagram a nested donation.  (Alternately, submit a
>.png file.)

使用了 LLM 生成。修改了其中的错误。

```ascii
场景：线程 A（优先级 10）持有锁 L1，线程 B（优先级 20）等待 L1 并持有锁 L2，
      线程 C（优先级 30）等待 L2

┌─────────────────────────────────────────────────────────────────┐
│                        优先级捐赠链                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Thread C          Thread B          Thread A                    │
│  (Priority 30)     (Priority 20)     (Priority 10)               │
│      ↓                 ↓                 ↓                        │
│  ┌─────────┐       ┌─────────┐       ┌─────────┐                │
│  │等待 L2  │──────▶│持有 L2  │       │持有 L1  │                │
│  │         │       │等待 L1  │──────▶│         │                │
│  └─────────┘       └─────────┘       └─────────┘                │
│       │                 │                 │                      │
│       ▼                 ▼                 ▼                      │
│  ┌─────────────────────────────────────────────────┐            │
│  │              优先级传递方向                      │            │
│  │    30 ──────▶ 30 ──────▶ 30                    │            │
│  │   (捐赠)      (捐赠)      (最终优先级)          │            │
│  └─────────────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────────────┘

数据结构表示：

Thread A:
┌───────────────────────────────────────┐
│ base_priority = 10                    │
│ donated_priority = 30  ◄───┐          │
│ priority = 30 (max)        │          │
│ waiting_lock = NULL        │          │
│ locks = [L1]               |          │
└────────────────────────────┼──────────┘
                             │      
Lock L1:                     │      
┌────────────────────────────┼──────────┐
│ holder = Thread A ◄────────┘          │
│ max_priority = 30                     │
│ semaphore = (value=0, waiters=[B])    │
└───────────────────────────────────────┘
                                    
Thread B:                           
┌───────────────────────────────────────────┐
│ base_priority = 20                        │
│ donated_priority = 30                     │
│ priority = 30 (max)  ◄────────────────────┼┐
│ waiting_lock = &L1                        |│
│ locks = [L2]                              │|
└───────────────────────────────────────────┘|
                                            │|
Lock L2:                                    │|
┌───────────────────────────────────────────┐|
│ holder = Thread B ◄───────────────────────┼┘
│ max_priority = 30                         │
│ semaphore = (value=0, waiters=[C])        │
└───────────────────────────────────────────┘

Thread C:
┌────────────────────────────────────────┐
│ base_priority = 30                     │
│ donated_priority = 0                   │
│ priority = 30                          │
│ waiting_lock = &L2                     │
│ locks = []                             │
└────────────────────────────────────────┘

如图，等待 L2 的线程的最大优先级为 30，捐献给持有 L2 的线程 B；同理，持有 L1 的线程 A 也将得到来自 L1 的捐赠。
```

#### ALGORITHMS

>B3: How do you ensure that the highest priority thread waiting for
>a lock, semaphore, or condition variable wakes up first?

类似 `sleeping_list` 的处理。信号量和条件变量中的 `waiters` 总是按优先级排序。

锁的处理则是使用优先级捐赠。使得等待锁的高优先级线程也能尽快开始处理。

>B4: Describe the sequence of events when a call to lock_acquire()
>causes a priority donation.  How is nested donation handled?

首先禁用中断，然后修改线程等待的锁。如果等待的锁被其他线程持有，那么就会发生优先级捐赠。接下来使用 `sema_down` 等待并获取锁，修改锁的信息即可。

这里可能会出现嵌套捐赠，设定一个最大深度迭代修改即可。

>B5: Describe the sequence of events when lock_release() is called
>on a lock that a higher-priority thread is waiting for.

先从当前线程持有的锁中删除这个锁，然后通过当前线程持有的其他锁更新该进程的捐赠优先级。接下来释放锁并恢复线程的基础优先级。

#### SYNCHRONIZATION

>B6: Describe a potential race in thread_set_priority() and explain
>how your implementation avoids it.  Can you use a lock to avoid
>this race?

优先级降低的时候可能会发生竞争。例如正确情况下，当前线程优先级降低，另外一个优先级更高的线程运行。然而发生中断后，检查的时候可能会基于旧状态，导致当前线程继续运行。同时优先级更高的线程也运行，发生竞争。

同样的只需要进入函数的时候禁用中断即可。

为什么不用锁：~~我们一定要用锁管理锁吗~~ 用锁保护锁会引入新的优先级问题；锁在中断上下文中不可使用，而中断上下文可能进行优先级更新操作。

#### RATIONALE

>B7: Why did you choose this design?  In what ways is it superior to
>another design you considered?

关闭中断可以在任何上下文中进行调用，安全便捷。使用锁的方法在上面已经当作错误示例进行了提问。

## Advanced Scheduler

#### DATA STRUCTURES

>C1: Copy here the declaration of each new or changed struct or struct member, global or static variable, typedef, or enumeration.  Identify the purpose of each in 25 words or less.

```c
// thread.h
struct thread
  {
    ...
    int nice;                           /**< Niceness. */
    fp64 recent_cpu;                    /**< Recent CPU usage. */
    ...
  }
```

`nice` 线程的友好值，越高越容易被分配到更少的 CPU 时间。   
`recent_cpu` 衡量线程最近占用的浮点数时间，是整数模拟的浮点数类型（25 字）。

```c
// fixedpoint.h
#ifndef FIXEDPOINT_H
#define FIXEDPOINT_H

#include <stdint.h>

#define F (1 << 14)  /*> 2^14 = 16384. Lowest 14 bits are seen as fractional bits. */

typedef int32_t fp64;  /*> 17.14 fixed-point number. */

#define INT_TO_FP(N)            ((N) * F)
#define FP_TO_INT_ZERO(N)       ((N) / F)
#define FP_TO_INT_NEAREST(N)    (((N) >= 0) ? ((N) + F / 2) / F : ((N) - F / 2) / F)
#define ADD_FP_INT(X, N)        ((X) + (N) * F)
#define SUB_FP_INT(X, N)        ((X) - (N) * F)
#define MUL_FP(X, Y)            (((int64_t)(X)) * (Y) / F)
#define DIV_FP(X, Y)            (((int64_t)(X)) * F / (Y))

#endif /**< threads/fixedpoint.h */
```

意图比较明显，不做解释。

```c
// thread.c
/** System load average. */
static fp64 load_avg;
```

`load_avg` 是 MLFQS 定义的系统平均负载。

#### ALGORITHMS

>C2: How is the way you divided the cost of scheduling between code
>inside and outside interrupt context likely to affect performance?

中断上下文中：分为高频和低频计算。高频运算就只是单纯更新当前线程的 `recent_cpu`；低频运算就只在每个时间片给线程列表排序，以更长的时间间隔更新 `load_avg`。

中断上下文外：直接取优先级最高的线程调度，所以速度比较快。具体做法看上面的回答。

#### RATIONALE

>C3: Briefly critique your design, pointing out advantages and
>disadvantages in your design choices.  If you were to have extra
>time to work on this part of the project, how might you choose to
>refine or improve your design?

优点就是写得没问题！

缺点是优先级更新的太慢了，并且随时都禁用中断可能没那么快，并且更新的频率固定。

有一个能尝试的改进方向就比如在线程比较多的时候更新快一点（动态调整更新频率），同时对每个线程有不同的优先级更新频率，比如优先级高的就更新快一点。

>C4: The assignment explains arithmetic for fixed-point math in
>detail, but it leaves it open to you to implement it.  Why did you
>decide to implement it the way you did?  If you created an
>abstraction layer for fixed-point math, that is, an abstract data
>type and/or a set of functions or macros to manipulate fixed-point
>numbers, why did you do so?  If not, why not?

我这么做了。因为把这些东西放在一起更符合 Pintos 的代码风格。选用宏是因为这样比函数快。至于 17.14 格式的小数，是因为文档要我这么写的。